.pio/
.vscode/

# Real channel keys never enter this repo — copy secrets.h.example instead.
**/secrets.h

# private-project task specs mirrored for mcpp attachment — canonical copies live in pac-garage-alarm
specs/garage-fw-*.md
